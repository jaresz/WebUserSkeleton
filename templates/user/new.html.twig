{% extends 'base.html.twig' %}

{% block title %}New User{% endblock %}

{% block body %}
    <h1>Create new User</h1>

    {{ include('user/_form.html.twig') }}

    <div class="pull-right" ><a href="{{ path('user_index') }}" class="btn btn-default">back to list</a>
    </div>
{% endblock %}